<div class="container">
  <div class="row">
    <h1>MY Tasks</h1>
  </div>
  <div class="row border-bottom py-5">
  <% @tasks.each do |task| %>
    <div class="col-6 flash flash-warning alert alert-dismissible fade show">
      <div class="d-flex align-items-center">
        <!-- <#%= link_to task_completed_path(task), method: :patch do %> -->
          <%= form_for(task, url: task_completed_path(task)) do |f| %>
             <%= f.check_box :completed, class: 'mr-3', onchange: "this.form.submit();" %>
          <% end %>
        <!-- <#% end %> -->
        <!-- <input type="checkbox" id="task_complete" name="task_complete" <%= "checked='checked'" if task.completed %> class="mr-2"> -->
        <span><strong><h3><%= task.title %></h3></strong></span>
      </div>
      <div class="align-items-center">
          <%= link_to "details", task_path(task), class: 'btn btn-primary mr-2' %>
          <%= link_to delete_task_path(task), method: :delete do%>
            <i class="fas fa-times"></i>
          <% end %>
        </a>
      </div>
    </div>
    <% end %>
  </div>
  <div>
    <%= link_to "add task", new_task_path %>
  </div>
</div>



